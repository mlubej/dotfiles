distributed:
  worker:
    profile:
      interval: 1h        # Time between statistical profiling queries
      cycle: 10h         # Time between starting new profile
    memory:
      terminate: 2.00  # fraction at which we terminate the worker